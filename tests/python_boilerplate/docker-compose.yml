version: '2'

services:
    python_boilerplate:
        build:
          context: .
        image: python_boilerplate:latest
        container_name: python_boilerplate
        ports:
          - "5000:5000"
        volumes:
         - .:/opt/python_boilerplate
